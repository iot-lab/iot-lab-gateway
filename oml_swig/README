1°) Install liboml : http://oml.mytestbed.net/attachments/download/767/oml2-2.9.0rc.tar.gz

2°) Install swig

3°) Swig mapping and compilation (swig interface oml.i) :
 
    $ swig -python -I/usr/local/include -I/usr/include oml.i 
    $ gcc -fPIC -I/usr/include/python2.6 -c oml_wrap.c 
    $ gcc -shared oml_wrap.o -loml2 -Wl,-rpath,/usr/local/lib -o _oml.so

4°) You can use the python test file :
   
    $ ./oml_test.py --oml-id 1 --oml-domain 445 # oml-id = node id & oml-domain = experiment id
